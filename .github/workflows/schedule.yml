name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.Leeyeji13 }}
          TIMEZONE: Asia/Seoul
